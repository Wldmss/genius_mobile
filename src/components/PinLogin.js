import React from 'react';
import { StyleSheet } from 'react-native';

const PinLogin = () => {
    return <View>PinLogin</View>;
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        border: `1px solid red`,
    },
});

export default PinLogin;
