import React from 'react';
import { StyleSheet } from 'react-native';

const BioLogin = () => {
    return <View>BioLogin</View>;
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        border: `1px solid red`,
    },
});

export default BioLogin;
